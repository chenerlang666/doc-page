import React from 'react';
import Layout from '@theme/Layout';

function Pre() {
    return (
        <Layout title="Presentations">

            <div
                style={{
                    display: 'flex',
                    justifyContent: 'center',
                    alignItems: 'center',
                    height: '50vh',
                    fontSize: '20px',
                }}>
                <p>
                    Edit <code>pages/presentations.js</code> and save to reload.
                </p>
            </div>
        </Layout>
    );
}

export default Pre;